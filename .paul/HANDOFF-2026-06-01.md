# PAUL Handoff

**Date:** 2026-06-01 10:27
**Status:** paused — massive session, context at 64%

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** base-v2 — Proactive context-injection engine for Claude Code
**Core value:** Injecting only the salient slice of context anchored on what Claude is touching, staying silent the rest of the time.

---

## Current State

**Version:** 0.0.0
**Phase:** 3 of 8 — Write Commands (CRUD) — **Complete ✓**
**Plan:** All plans closed. Ready for Phase 4.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Loop closed]
```

---

## What Was Done (This Session)

- **Phase 1: Hook Engine** — session-start, post-tool-use, user-prompt-submit hooks. Configurable namespace (base.toml), configurable SPARQL queries (queries.toml). Fail-open. 21 tests. Commit `bfe0a6a`.
- **Phase 2: Domain Matcher** — domains.toml parser, multi-signal matcher (keyword/path/always-on/exclude/sticky), session dedup via .base/.session with rules-hash. 44 tests. Commit `94ca89e`.
- **Phase 3: Write Commands** — CRUD infrastructure + all 6 entity types (project, task, decision, entity, goal, reminder) via SPARQL UPDATE. 66 tests. Commit `80b2c43`.
- **Key design decisions:** namespace configurable, session-end nudge dropped, no dirty-file tracking (mtime self-healing), session dedup via rules-hash, domains.toml format.
- Chris wants open-ontologies AST extraction integrated in Phase 4 (extractor.py + onto_ast.py scripts).

---

## What's Next

**Immediate:** `/paul:plan` for Phase 4 (Extraction Layer)

Phase 4 scope from ROADMAP:
- `base sync` command
- Markdown frontmatter → triples (MOP port to Rust)
- paul.json → triples plugin
- Re-scan = overwrite-in-place (no dup/drift)
- **PLUS:** Chris wants open-ontologies AST extraction integrated (extractor.py + onto_ast.py → OWL ontology)
- Incremental extraction uses mtime vs ops:lastExtracted (no dirty-file tracking)

**After that:** Phase 5 (Signal Layer) — the proof that SPARQL-filtered injection beats v1's firehose.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/PROJECT.md` | Requirements, decisions, validated features |
| `.paul/ROADMAP.md` | Phase overview (4 of 9 complete) |
| `src/` | All Rust source (hook/, domain/, crud/, config, store, ontology) |
| `docs/settings-hook-config.md` | Settings.json wiring docs |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or `/paul:plan`
3. Phase 4 is next — Extraction Layer

---

*Handoff created: 2026-06-01 10:27*

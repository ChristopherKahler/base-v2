# PAUL Handoff

**Date:** 2026-06-01 11:46
**Status:** paused — 5 phases shipped in one session, context at 54%

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** base-v2 — Proactive context-injection engine for Claude Code
**Core value:** Injecting only the salient slice of context anchored on what Claude is touching, staying silent the rest of the time.

---

## Current State

**Version:** 0.0.0
**Phase:** 5 of 8 — Signal Layer — **Complete ✓**
**Plan:** All loops closed. Ready for Phase 6.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Loop closed]
```

**Milestone:** [██████░░░░] 66% — 6 of 9 phases complete

---

## What Was Done (This Session — June 1, 2026)

5 phases shipped, 84 tests passing, all clippy clean:

| Phase | What | Tests | Commit |
|-------|------|-------|--------|
| 1 | Hook Engine — session-start, post-tool-use, user-prompt-submit hooks. Configurable namespace (base.toml), configurable SPARQL queries (queries.toml). Fail-open. | 21 | `bfe0a6a` |
| 2 | Domain Matcher — domains.toml parser, multi-signal matcher (keyword/path/always-on/exclude/sticky), session dedup via .base/.session with rules-hash. | 44 | `94ca89e` |
| 3 | Write Commands (2 plans) — CRUD infrastructure + all 6 entity types (project, task, decision, entity, goal, reminder) via SPARQL UPDATE. | 66 | `80b2c43` |
| 4 | Extraction Layer — `base sync` with frontmatter + paul.json extractors, mtime incremental, configurable include/exclude. | 77 | `6368a6b` |
| 5 | Signal Layer — 3 signal types (active-awareness, pulse, staleness) with cross-session hash dedup, budget cap. Session-start uses signals as primary injection, ad-hoc queries as fallback. | 84 | `a90fbbe` |

**Key design decisions made this session:**
- Namespace URI + prefix runtime-configurable via base.toml
- Session-end nudge dropped (unreliable hook)
- No dirty-file tracking — mtime vs ops:lastExtracted self-healing
- Session dedup via rules-hash (.base/.session)
- Signal state persists across sessions (novelty is cross-session)
- Priority 1 signal never dropped by budget cap
- Chris wants open-ontologies AST extraction (deferred — extraction infra supports it)

---

## What's Next

**Immediate:** `/paul:plan` for Phase 6 (CARL Absorption)

Phase 6 scope from ROADMAP:
- Port dedup signatures, rule priority
- DEVMODE dashboard output
- PSMM coupling
- Reconcile CARL domains ↔ BASE workspaces/projects into one model
- Retire Python CARL hooks

**⚠️ Research likely needed:** Reconciling CARL's domain/rule model with BASE's graph entities. CARL domains are currently in carl.json (Python). Phase 2 already built the Rust domain matcher. Phase 6 bridges the gap — port CARL's remaining mechanisms (DEVMODE, PSMM, context brackets) and retire the Python implementation.

**After Phase 6:**
- Phase 7: v1 Migration + Cutover (JSON→triples, retire v1 MCP/JSON)
- Phase 8: Dashboard (optional, SPARQL-backed views)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/PROJECT.md` | Requirements, decisions, validated features |
| `.paul/ROADMAP.md` | Phase overview (6 of 9 complete) |
| `src/hook/` | Hook handlers (session_start, post_tool_use, user_prompt_submit) |
| `src/domain/` | Domain matching + session dedup |
| `src/crud/` | CRUD for 6 entity types |
| `src/extract/` | File-to-graph sync (frontmatter, paul.json) |
| `src/signal/` | Signal layer (active-awareness, pulse, staleness, suppression) |
| `src/config.rs` | BaseConfig, NamespaceConfig, SyncConfig, SignalConfig |
| `src/store.rs` | Oxigraph load/query/write_back |
| `src/ontology/` | ops.ttl vocabulary + namespace-aware loader |
| `docs/settings-hook-config.md` | Settings.json wiring docs |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume`
3. Phase 6 (CARL Absorption) is next — consider `/paul:discuss` or `/paul:research-phase` first since ROADMAP flags this as "Research: Likely"

---

*Handoff created: 2026-06-01 11:46*

---
phase: 08-command-center-dashboard
plan: 08
type: execute
autonomous: true
---

<objective>
## Goal
Dashboard polish batch — fix session grouping, enrich hook event data, redesign PAUL Usage panel, fix project name resolution, add project open/closed toggle.
</objective>

<context>
@.paul/PROJECT.md
</context>

<acceptance_criteria>
## AC-1: Session Activity groups correctly by session_id
Given multiple Claude Code sessions are running
When events are logged to hook-events.jsonl
Then each session gets its own card (not piled into one)

## AC-2: Hook events log BASE signal data
Given a pre-tool-use or post-tool-use hook fires
When BASE injects domains, AST maps, or intercepts grep
Then the event log includes domains_matched, ast_injected, grep_intercepted, section_context fields

## AC-3: PAUL Usage panel shows project hierarchy
Given ledger entries exist
When the user opens PAUL Usage tab
Then projects drill down: Project → Phase → Plan → Actions with durations and costs

## AC-4: Projects card has Open/Closed toggle
Given completed projects exist
When the user views Operations → Projects
Then only Open projects show by default, Closed toggle reveals completed ones
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Session ID fix + multi-expand + signal data + PAUL Usage redesign + project toggle</name>
  <files>src/hook/mod.rs, src/hook/pre_tool_use.rs, src/hook/post_tool_use.rs, src/hook/user_prompt_submit.rs, src/extract/ledger.rs, dashboard/src/panels/SessionActivity.svelte, dashboard/src/panels/CostAttribution.svelte, dashboard/src/panels/UsageAnalytics.svelte, dashboard/src/panels/OperationsPanel.svelte, dashboard/src/app.css</files>
  <action>All changes executed inline during session</action>
  <verify>npm run build clean, cargo build clean, dashboard functional</verify>
  <done>AC-1 through AC-4 satisfied</done>
</task>
</tasks>

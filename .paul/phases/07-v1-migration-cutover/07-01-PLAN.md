---
phase: 07-v1-migration-cutover
plan: 01
status: complete
type: retroactive
created: 2026-06-02T08:14:00-05:00
---

# Phase 7 Plan 01: CRUD Infrastructure + Test Isolation

<objective>
Fix slug/name resolution mismatch across all CRUD commands, add Milestone entity type for 3-tier project hierarchy (Project → Milestone → Task), and resolve 4 pre-existing test failures caused by global config leakage into test isolation.
</objective>

<boundaries>
- Slug resolution: support both display name and slug for all get/update/done commands
- Milestone: full CRUD (add, list, get, update) with project linkage and task grouping
- Task: add optional --milestone parameter for add and list
- Test fixes: isolate sync tests from ~/.base-gbl/ global config
- No changes to hook engine, signals, or extraction layer
</boundaries>

<tasks>
- Task 1: Add resolve_slug() to crud/mod.rs — tries slug, slugified input, then SPARQL name lookup
- Task 2: Wire resolve() helper into CLI dispatch for project, entity, goal, milestone, reminder
- Task 3: Create crud/milestone.rs with full CRUD (add, list, get, update)
- Task 4: Add MilestoneAction to CLI with add/list/get/update subcommands
- Task 5: Update task.rs — add optional milestone_slug to add() and list()
- Task 6: Fix 3 sync tests — extract sync_domain_list() for test isolation from global config
- Task 7: Fix domain_cli_test — assert on specific domain, not total count
- Task 8: Update all test call sites for new task::add signature
</tasks>

<acceptance_criteria>
- AC-1: `base project update "Hunter Exotics" --status completed` resolves display name to slug
- AC-2: `base milestone add --project chris-ai-website --name "Full Refactor"` creates milestone
- AC-3: `base task add --project X --name Y --milestone Z` links task to milestone
- AC-4: `base task list --milestone Z` filters by milestone
- AC-5: All 117 tests pass (0 failures)
- AC-6: Binary installed to ~/.local/bin/base
</acceptance_criteria>
